<nav class="bg-white dark:bg-gray-900">
  <div class="flex justify-between p-4 text-lg">
    <a href="/" class="font-handjet text-2xl text-gray-900 transition-colors dark:text-gray-200">مين اسرع</a>
    <div class="space-x-4 space-x-reverse">
      <% if (!locals.user) { %>
      <!-- -->
      <% if (locals.path !== "/login") { %>
      <a href="/login" class="text-gray-700 transition-colors hover:text-gray-900 hover:underline dark:text-gray-300 hover:dark:text-white">تسجيل الدخول</a>
      <% } %>
      <!-- -->
      <% if (locals.path !== "/register") { %>
      <a href="/register" class="text-gray-700 transition-colors hover:text-gray-900 hover:underline dark:text-gray-300 hover:dark:text-white">إنشاء حساب</a>
      <% } %>
      <!-- -->
      <% } else { %>
      <div class="flex items-center space-x-2">
        <div class="flex items-center space-x-2">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="h-4 w-5 text-purple-500">
            <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 6a3.75 3.75 0 1 1-7.5 0 3.75 3.75 0 0 1 7.5 0ZM4.501 20.118a7.5 7.5 0 0 1 14.998 0A17.933 17.933 0 0 1 12 21.75c-2.676 0-5.216-.584-7.499-1.632Z" />
          </svg>
          <span class="mr-2 text-gray-900 dark:text-gray-200"><%= user.username %></span>
        </div>
        <a href="/logout" class="text-gray-700 transition-colors hover:text-gray-900 hover:underline dark:text-gray-300 hover:dark:text-white">تسجيل خروج</a>
      </div>
      <% } %>
    </div>
  </div>
</nav>
