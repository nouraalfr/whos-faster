<!doctype html>
<html lang="ar" dir="rtl" class="h-full">
  <%- include('head.ejs') -%>

  <body class="relative min-h-full overflow-hidden bg-gray-50 text-gray-800 dark:bg-gray-900 dark:text-white">
    <div class="container relative z-10 mx-auto space-y-16 px-4 py-16">
      <header class="text-center">
        <h1 class="text-4xl font-bold tracking-tight md:text-4xl lg:text-5xl">مين اسرع؟</h1>
        <p class="mt-4 text-lg leading-relaxed md:text-xl lg:text-2xl">
          مين اسرع هي لعبة تقيس سرعة كتابتك للغة العربية، افرد عضلات اصابعك
          <a class="font-bold text-indigo-600 underline hover:text-indigo-800 dark:text-indigo-400 dark:hover:text-indigo-300" href="/play"> وابدأ </a>
        </p>
      </header>

      <% if (locals.attempts) { %>
      <section class="relative flex justify-center">
        <div class="pointer-events-none absolute inset-0 -z-10">
          <% for (let i = 0; i < 100; i++) { %>
          <svg class="absolute" style="top: <%= Math.random() * 100 %>%; left: <%= Math.random() * 100 %>%; animation: sparkle <%= Math.random() * 3 + 2 %>s infinite" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="<%= Math.random() > 0.5 ? '#FFD700' : '#FFFFE0' %>" width="<%= Math.random() * 10 + 5 %>" height="<%= Math.random() * 10 + 5 %>">
            <circle cx="12" cy="12" r="4" />
          </svg>
          <% } %>
        </div>
        <div class="w-full max-w-4xl">
          <table class="mx-auto w-full overflow-hidden rounded-lg border border-purple-500 text-center shadow-md">
            <caption class="bg-purple-500 p-4 text-lg font-semibold text-white md:text-xl lg:text-2xl">
              اسرع ناس بالعالم
            </caption>
            <thead class="bg-purple-200 dark:bg-purple-700">
              <tr class="border-b border-purple-300 dark:border-purple-600">
                <th class="p-4 text-sm font-medium text-purple-800 md:text-base lg:text-lg dark:text-purple-100">البطل</th>
                <th class="p-4 text-sm font-medium text-purple-800 md:text-base lg:text-lg dark:text-purple-100">كلمة في الدقيقة</th>
              </tr>
            </thead>
            <tbody>
              <% attempts.forEach(attempt => { %>
              <tr class="bg-white odd:bg-gray-100 hover:bg-gray-200 dark:bg-gray-800 dark:odd:bg-gray-700 dark:hover:bg-gray-600">
                <td class="p-4 text-sm font-medium md:text-base lg:text-lg"><%= attempt.username %></td>
                <td class="p-4 text-sm font-medium md:text-base lg:text-lg"><%= attempt.top %></td>
              </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </section>
      <% } %>
    </div>
  </body>
</html>
